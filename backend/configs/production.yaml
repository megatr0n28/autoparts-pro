app:
  name: AutoParts Pro API
  environment: production
  port: 8080

log:
  level: info

database:
  host: postgres
  port: 5432
  name: autoparts
  user: postgres

redis:
  host: redis
  port: 6379

jwt:
  secret: ${JWT_SECRET}
  expiration: "15m"
  refresh_expiration: "720h"